<UserControl x:Class="Samba.Modules.PackageServiceModule.PackageServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Samba.Modules.PackageServiceModule"
             xmlns:cal="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:PackageServiceViewModel}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding HeaderText}"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="#FF2B2B2B"
                   Margin="0,0,0,5" />

        <TextBlock Grid.Row="1"
                   Text="{Binding Description}"
                   TextWrapping="Wrap"
                   Foreground="#FF4B4B4B"
                   Margin="0,0,0,15" />

        <Border Grid.Row="2"
                BorderBrush="#33000000"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10"
                Background="#0F000000">
            <!--  ViewModel'den view ,  bir Prism region -->
            <ContentControl cal:RegionManager.RegionName="PackageServiceEntityRegion" />
        </Border>
    </Grid>
</UserControl>
