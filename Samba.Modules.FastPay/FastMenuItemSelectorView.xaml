<UserControl x:Class="Samba.Modules.FastPayModule.FastMenuItemSelectorView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:FxButton="clr-namespace:FlexButton;assembly=FlexButton"
             xmlns:UIControls="clr-namespace:Samba.Presentation.Controls.UIControls;assembly=Samba.Presentation.Controls"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ItemsControl MaxHeight="70" Grid.Row="0" ItemsSource="{Binding MostUsedMenuItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" Columns="{Binding MostUsedItemsCategory.ColumnCount}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <FxButton:FlexButton Margin="5,0,0,7" Focusable="False" 
                                         ButtonColor="{Binding ButtonColor, Converter={StaticResource NullBrushConverter}}"
                                         ButtonImage="{Binding ImagePath}" Content="{Binding Caption}" Command="{Binding Command}"                        
                                         CommandParameter="{Binding ScreenMenuItem}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Name="MainGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="25*" />
                <ColumnDefinition Width="75*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" Name="SelectedItemsRow" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="60*" Name="MenuItemRow" />
                <RowDefinition Height="40*" Name="NumeratorRow" />
            </Grid.RowDefinitions>

            <!-- Categories ScrollViewer -->
            <ScrollViewer Grid.RowSpan="4" Grid.Column="0" Margin="3,0,4,1" UIControls:KineticBehaviour.HandleKineticScrolling="True"
                 VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Categories,NotifyOnTargetUpdated=True}" TargetUpdated="ItemsControl_TargetUpdated">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding CategoryColumnCount}" VerticalAlignment="{Binding CategoriesVerticalAlignment}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <FxButton:FlexButton FontSize="{Binding FontSize}" Content="{Binding Caption}" Focusable="False" Command="{Binding ButtonCommand}"
                                 CommandParameter="{Binding Category}" Height="{Binding MButtonHeight}" Margin="0,1,1,1"
                                 ButtonImage="{Binding ImagePath}" ButtonColor="{Binding MButtonColor,Converter={StaticResource NullBrushConverter}}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Selected items panel -->
            <Grid Grid.Column="1" Visibility="{Binding IsSelectedItemsVisible,Converter={StaticResource VisibilityConverter}}" Margin="0,0,0,3" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border BorderThickness="1" BorderBrush="Silver">
                    <ListBox BorderThickness="0" ItemsSource="{Binding SelectedMenuItems}" UIControls:ListBoxBehavior.ScrollOnNewItem="true">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <TextBlock FontSize="18" MinWidth="20" Text="{Binding Quantity}"></TextBlock>
                                    <TextBlock FontSize="18" Text="{Binding ScreenMenuItem.Caption}"></TextBlock>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
                <StackPanel Grid.Column="1">
                    <FxButton:FlexButton Command="{Binding CloseMenuViewCommand}" Content="{Binding CloseMenuViewCommand.Caption}" 
                                         Height="40" ButtonColor="Red" Margin="3,0"/>
                </StackPanel>
            </Grid>

            <!-- Numerator and subcategories panels remain unchanged -->

            <!-- QuickNumerator ListBox -->
            <ListBox MinHeight="60" Grid.Row="1" Grid.Column="1" BorderThickness="0" Background="Transparent"
                 Visibility="{Binding IsQuickNumeratorVisible,Converter={StaticResource VisibilityConverter}}"
                 ItemsSource="{Binding QuickNumeratorValues}" SelectedItem="{Binding QuickNumeratorValue,Mode=TwoWay}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Viewbox>
                                <TextBlock FontWeight="Bold" Text="{Binding}" />
                            </Viewbox>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.Resources>
                    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#888" />
                    <Style TargetType="ListBoxItem">
                        <Setter Property="SnapsToDevicePixels" Value="true" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border Margin="0,0,4,4" Background="WhiteSmoke" Name="Border" BorderThickness="1"
                                         CornerRadius="4" BorderBrush="Gray">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="Border" Property="BorderThickness" Value="2" />
                                            <Setter Property="Foreground" Value="Red" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="false">
                                            <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.Resources>
            </ListBox>

            <!-- Remaining menu items, subcategories, and numberpad sections are unchanged -->
        </Grid>
    </Grid>
</UserControl>
